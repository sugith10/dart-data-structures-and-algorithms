class Node{
  int data;
  Node? next;
  Node(this.data);
}

class Stack { 
  int size;
  int current = 0;
  Stack(this.size);

  Node? tail;

  add(int data){
    if(current < size){
      if(tail == null){

      }
    }
  }

}